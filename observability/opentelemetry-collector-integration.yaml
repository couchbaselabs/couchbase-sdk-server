receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:10003

  hostmetrics:
    scrapers:
      load:
      cpu:
      disk:
      filesystem:
      memory:
      network:
      paging:
      process:

extensions:
  health_check:
  pprof:
  zpages:

processors:
  batch:

exporters:
  logging:
    loglevel: debug

  jaeger:
    endpoint: jaeger:14250
    tls:
      insecure: true

  # This is an endpoint exposed by the collector, for Prometheus to scrape
  prometheus:
    endpoint: "0.0.0.0:10000"

  otlp/honeycomb:
    endpoint: "api.honeycomb.io:443"
    headers:
      "x-honeycomb-team": "bQMef6WpRjOr46OL6SDTHM"
      "x-honeycomb-dataset": "sdk.integration"

  otlp/tempo:
    endpoint: tempo:4317
    tls:
      insecure: true

service:
  extensions: [health_check,pprof,zpages]
  pipelines:

    traces:
      receivers: [otlp]
      exporters: [otlp/tempo,otlp/honeycomb,jaeger]
    metrics:
      receivers: [otlp]
      exporters: [prometheus]
